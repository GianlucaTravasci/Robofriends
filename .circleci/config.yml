version: 2.1
jobs:
  build:
    docker: 
      - image: circleci/node:14.10.1 # the primary container, where your job's commands are run
    steps:
      - checkout # check out the code in the project directory
      - run: echo "Building phase" # run the `echo` command
      - run: npm install
      - run: CI=true npm run build
  test:
    docker: 
      - image: circleci/node:14.10.1 
    steps:
      - checkout
      - run: echo "Test phase"
      - run: npm install
      - run: npm test
  hithere:
    docker: 
      - image: circleci/node:14.10.1
    steps:
      - checkout
      - run: echo "Hiiiiiiiithere!"
workflows:
  version: 2.1
  build_test_lint:
    jobs:
      - build
      - hithere
      - test:
        requires:
          -hithere
