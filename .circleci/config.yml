version: 2.1
jobs:
  bobby:
    docker: 
      - image: cimg/node:14.10.1 # the primary container, where your job's commands are run
    steps:
      - checkout # check out the code in the project directory
      - run: echo "hello bobby" # run the `echo` command
  sally:
    docker: 
      - image: cimg/node:14.10.1 # the primary container, where your job's commands are run
    steps:
      - checkout # check out the code in the project directory
      - run: echo "hello sally" # run the `echo` command
workflow:
  version: 2.1
  bobby_sally:
    jobs:
      - bobby
      - sally
